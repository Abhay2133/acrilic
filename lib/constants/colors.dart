import 'package:flutter/widgets.dart';

class AppColor {
  // Background
  static const Color primaryBackground = Color(0xFF171212);

  // Text
  static const Color primaryText = Color(0xFFDACACA);

  // Button
  static const Color primaryButton = Color(0xFF834C3D);
}